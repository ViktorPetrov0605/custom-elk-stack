[project]
name = "browseruse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "browser-use>=0.11.9",
    "playwright>=1.58.0",
]
