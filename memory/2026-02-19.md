# Memory - 2026-02-19

## Session Summary
Focused on auditing and reorganizing the `custom-elk-stack` repository to transition from a legacy Elastiflow structure to a production-grade Unified Logstash collector setup.

## Key Accomplishments
- **Production Sync**: Logged into production backend servers (10.4.4.21) to extract the active `logstash-unified.conf` and verified it contains the 4096x Juniper NetFlow sampling multiplier.
- **Security Scrubbing**: Identified and removed hardcoded SSH passwords (`T3l3h0us#`) from maintenance scripts (`scripts/check_elastiflow.sh`) and sanitized internal credentials in configuration templates.
- **KISS Reorganization**: Restructured the GitHub repository `ViktorPetrov0605/custom-elk-stack` into a clean architecture:
  - `/deploy`: Docker volumes, compose files, and discovery configs.
  - `/logstash/pipeline`: Unified `logstash.conf`.
  - `/dashboards`: Curated production `.ndjson` exports.
  - `/_archive`: Legacy/temporary files.
- **GitHub Update**: Pushed all refactored changes (Commit: `7bd68ee`).

## Decisions & Context
- **Preferred Config**: Chose the `logstash-migration/logstash-unified-flow.conf` logic as the base for the new `logstash.conf` because it uses ECS-compliant `@metadata` and handles dynamic sampling rates more cleanly than the root version.
- **Credential Management**: Transitioned all secrets to a template-based `env.example` to prevent future leakage.

## Technical Details
- **ELK Version**: 9.2.4
- **Sampling Multiplier**: 4096 (applied to `network.bytes` and `network.packets` for Juniper devices).
- **Architecture**: 3-node distributed cluster (10.4.4.87, 10.4.4.21, 10.4.4.90).
