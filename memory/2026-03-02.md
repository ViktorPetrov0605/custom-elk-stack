# Session Log - 2026-03-02

## Team & Authorization
- **Slav Kolev** (ID: 8350718125, @slavkolev3) added to authorized team members list.
- Updated `MEMORY.md`, `USERS.md`, and `BACKUP_README.md` with Slav's details.

## CTF Competition (CTF-2026)
- Started CTF with Viktor.
- Goal: Get the prize and buy a new server for Valentin-bot.
- Current observations: 
    - `slav.gay` is live with a "Somnos Services" retro dashboard.
    - User Slav Kolev requested "Supportivo" config on `10.33.0.82` (TH3-BG2) using RADIUS credentials.

## Network & Infrastructure
- Viktor moved the ELK test network to a new range; currently in **advisory-only** mode for ELK tasks as hosts are unreachable from my local network.
- **Traceroute Diagnosis**: Traceroute to `10.33.0.82` (TH3-BG2) fails after the gateway `10.4.4.253`.

## ELK Technical Details
- Clarified index sizing: With `max_primary_shard_size: 10gb` and `number_of_shards: 2`, an index rolls over at **20GB** of primary data.
- Footprint with 1 replica: Total disk usage is **40GB** per 10GB-primary-shard index (2 P + 2 R).
- Confirmed rollover naming (`-00000X`) remains active across days until the size/age limit is met.

## Maintenance
- Updated local `BACKUP_README.md` and repo `README.md` to keep team list synchronized.
- Pushed team updates to GitHub `custom-elk-stack` repo.
